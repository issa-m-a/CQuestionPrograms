#include<stdio.h>
#include<stdlib.h>

double numberOfStudents(double);
double getMarks(double);
double calculateMean(double);
double validateInput(double);
double compute(double);
int main()
{
    int number,count;
    printf("Enter the number of students:");
    scanf("%d",&number);
    for (count = 0; count < number; count++)
    {
        
    }
    /*what this code does is that the count is used to initialise the loop and the loop only works
    if the count is less than the number*/
    return 0;
}


